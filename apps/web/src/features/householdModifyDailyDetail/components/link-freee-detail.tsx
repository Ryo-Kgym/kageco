import { LinkFreeeComponent } from "../../freeeAuth/components/link-freee-component";

export const LinkFreeeDetail = ({
  id,
  onClose,
}: {
  id: string;
  onClose: () => void;
}) => {
  return <LinkFreeeComponent>test</LinkFreeeComponent>;
};
