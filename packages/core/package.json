{
  "name": "@/core",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "with-env": "dotenv -e ../../.env --",
    "test": "vitest --run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@acme/tsconfig": "workspace:^",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@/util": "workspace:^"
  }
}
